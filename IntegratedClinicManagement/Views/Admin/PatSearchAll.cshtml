@model ClinicManagementProject.Models.Patient

@{
    ViewData["Title"] = "Patient Search";
    Layout = "~/Views/Admin/_AdminLayout.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />

    <title>Patient Registration</title>
    <style>
        form {
            width: 300px;
        }
    </style>
    <script>//function passwordCheck() {
            //    var pass = document.getElementById("Password").value;
            //    var repass = document.getElementById("txtReType").value;
            //    if (pass != repass)
            //        alert("Password Mismatch");
            //}</script>
</head>
<body>

    <h1 id="main" class="alert alert-success">Patient Directory</h1>
    <hr />
    <div>@Html.ActionLink("View All Patient", "PatViewAll", "Admin")</div>
    <div class="row">
        <div class="col-md-4">
            @using (Html.BeginForm("PatSearchAll", "Admin", FormMethod.Post))
            {
                @Html.LabelFor(m => m.Name, htmlAttributes: new { @class = "form-control" })
                @Html.EditorFor(m => m.Name, new { htmlAttributes = new { @class = "form-control" } })

                <br />
                <button class="btn btn-primary">Search Patient</button>
            }
        </div>
    </div>
    <section>
        @Html.ActionLink("Back to Main Page", "PatMain", "Admin")
    </section>


    @section Scripts {
        @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    }
</body>
</html>